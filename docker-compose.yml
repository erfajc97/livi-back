services:
  api:
    build: .
    container_name: api-ecommerce
    env_file: .env
    ports:
      - "${PORT:-4001}:${PORT:-4001}"
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      PORT: ${PORT:-4001}
      # Compose service hostname (must match this service's key: `db`)
      DB_HOST: db
      DB_PORT: 5432
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  db:
    image: postgres:16-alpine
    container_name: api-ecommerce-db
    environment:
      POSTGRES_USER: ${DB_USERNAME:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      # POSTGRES_DB solo aplica la PRIMERA vez que se crea el volumen.
      # Cambiar DB_NAME después no mueve los datos: el API miraría una base vacía.
      POSTGRES_DB: ${DB_NAME:-postgres}
    # Solo localhost: si se publica 0.0.0.0:5432 con password débil, cualquiera
    # en internet puede vaciar el volumen. El API habla por la red interna `db`.
    ports:
      - "127.0.0.1:${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped

volumes:
  postgres_data:
